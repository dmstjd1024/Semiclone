  
spring:
  datasource:
    url: jdbc:mariadb://localhost:33076/semiclone?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password : 1234

  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true

  devtools:
    livereload:
      enabled: true
  thymeleaf:
    cache: false
  messages:
    basename: validation


server:
  port: 8080
